# Windows 防火墙说明

如果本机能打开 `http://localhost:3000`，但局域网其他电脑打不开，一般是防火墙没有放行 3000 端口。

建议使用“高级安全 Windows Defender 防火墙”新增入站规则：

1. 选择“入站规则”
2. 选择“新建规则”
3. 规则类型选择“端口”
4. 协议选择“TCP”
5. 特定本地端口填写 `3000`
6. 操作选择“允许连接”
7. 配置文件只勾选“专用”
8. 名称填写 `设计任务执行台 3000`

这样比在“允许应用通过防火墙”里反复勾选 Node.js 更干净。
