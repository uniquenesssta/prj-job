# Mac 启动说明

双击根目录下的 `scripts/start-mac.command`。

如果 macOS 提示没有权限运行，打开“终端”，进入项目根目录后执行：

```sh
chmod +x scripts/start-mac.command
```

之后再双击 `scripts/start-mac.command`。

## Node.js

Mac 需要先安装 Node.js。推荐安装 LTS 版本：

```text
https://nodejs.org/
```
