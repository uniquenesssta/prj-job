# Windows 启动说明

双击根目录下的 `scripts/start-windows.bat`。

启动窗口不要关闭。窗口中会显示：

- 本机地址：`http://localhost:3000`
- 局域网地址：类似 `http://192.168.x.x:3000`

如果其他电脑打不开，请查看 `docs/firewall.md`。

## Node.js

电脑需要安装 Node.js。推荐安装 LTS 版本：

```text
https://nodejs.org/
```
